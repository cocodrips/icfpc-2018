{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.279.1",
    "puppeteer": "^1.6.0",
    "request": "^2.87.0",
    "request-promise-native": "^1.0.5"
  }
}
